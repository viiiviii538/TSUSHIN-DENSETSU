<Window x:Class="TsushinDensetsu.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tsushin Densetsu"
        Height="450"
        Width="800">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding WelcomeMessage}"
                   FontSize="24"
                   FontWeight="Bold" />
        <TabControl Grid.Row="1" Margin="0,16,0,0">
            <TabItem Header="ダッシュボード">
                <Grid Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel>
                        <TextBlock Text="{Binding SecurityStatus}"
                                   FontSize="16" />
                        <TextBlock Text="{Binding NetworkOverview}"
                                   FontSize="14"
                                   Margin="0,8,0,0"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                    <GroupBox Grid.Row="1" Header="Network Devices" Margin="0,16,0,0">
                        <ListView ItemsSource="{Binding NetworkDevices}">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="200" />
                                    <GridViewColumn Header="IP Address" DisplayMemberBinding="{Binding IpAddress}" Width="150" />
                                    <GridViewColumn Header="Role" DisplayMemberBinding="{Binding Role}" Width="150" />
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </GroupBox>
                </Grid>
            </TabItem>
            <TabItem Header="スピードテスト" DataContext="{Binding SpeedTest}">
                <Grid Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Button Content="計測開始"
                            Command="{Binding StartTestCommand}"
                            HorizontalAlignment="Left"
                            Padding="20,8"
                            FontSize="16" />
                    <TextBlock Grid.Row="1"
                               Text="{Binding StatusMessage}"
                               Margin="0,12,0,0"
                               FontSize="14"
                               TextWrapping="Wrap" />
                    <Grid Grid.Row="2" Margin="0,24,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0"
                                   Text="ダウンロード"
                                   FontWeight="Bold"
                                   Margin="0,0,12,8" />
                        <TextBlock Grid.Row="0" Grid.Column="1"
                                   Text="{Binding DownloadDisplay}"
                                   Margin="0,0,0,8" />
                        <TextBlock Grid.Row="1"
                                   Text="アップロード"
                                   FontWeight="Bold"
                                   Margin="0,0,12,8" />
                        <TextBlock Grid.Row="1" Grid.Column="1"
                                   Text="{Binding UploadDisplay}"
                                   Margin="0,0,0,8" />
                        <TextBlock Grid.Row="2"
                                   Text="Ping"
                                   FontWeight="Bold"
                                   Margin="0,0,12,8" />
                        <TextBlock Grid.Row="2" Grid.Column="1"
                                   Text="{Binding PingDisplay}"
                                   Margin="0,0,0,8" />
                        <TextBlock Grid.Row="3"
                                   Text="ジッター"
                                   FontWeight="Bold"
                                   Margin="0,0,12,0" />
                        <TextBlock Grid.Row="3" Grid.Column="1"
                                   Text="{Binding JitterDisplay}" />
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
