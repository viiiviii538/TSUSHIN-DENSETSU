<Window x:Class="TsushinDensetsu.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tsushin Densetsu"
        Height="450"
        Width="800">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding WelcomeMessage}"
                   FontSize="24"
                   FontWeight="Bold" />
        <StackPanel Grid.Row="1" Margin="0,16,0,0">
            <TextBlock Text="{Binding SpeedTestSummary}"
                       FontSize="16" />
            <TextBlock Text="{Binding SecurityStatus}"
                       FontSize="16"
                       Margin="0,8,0,0" />
            <TextBlock Text="{Binding NetworkOverview}"
                       FontSize="14"
                       Margin="0,8,0,0" />
        </StackPanel>
        <GroupBox Grid.Row="3" Header="Network Devices" Margin="0,16,0,0">
            <ListView ItemsSource="{Binding NetworkDevices}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="200" />
                        <GridViewColumn Header="IP Address" DisplayMemberBinding="{Binding IpAddress}" Width="150" />
                        <GridViewColumn Header="Role" DisplayMemberBinding="{Binding Role}" Width="150" />
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>
    </Grid>
</Window>
